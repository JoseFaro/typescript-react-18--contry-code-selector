import ReactProps from '../../config/reactFC.type';
import SelectorController from './selector.controller';
import './selector.scss';

const Selector: ReactProps = () => {
  SelectorController();

  return <>Selector</>;
};

export default Selector;
