import ReactProps from '../../../config/types';
import SelectorItemController from './selectorItem.controller';
import './selectorItem.scss';

const SelectorItem: ReactProps = () => {
  SelectorItemController();

  return <>SelectorItem</>;
};

export default SelectorItem;
